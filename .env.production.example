# Copy to ".env" at the project root (docker-compose.prod.yml and the
# Caddyfile both read from it). Never commit the real ".env" file.

DOMAIN=vie-heureux.blog
ACME_EMAIL=you@example.com

POSTGRES_USER=siterelation
POSTGRES_PASSWORD=change-me-to-a-long-random-password
POSTGRES_DB=siterelation
